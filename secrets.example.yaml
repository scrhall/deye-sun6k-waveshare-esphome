# Copy this file to secrets.yaml. Never commit secrets.yaml.
wifi_ssid: "YOUR_WIFI"
wifi_password: "YOUR_WIFI_PASSWORD"

# Generate with: openssl rand -base64 32
api_encryption_key: "REPLACE_WITH_A_32_BYTE_BASE64_KEY"

# Generate with: openssl rand -hex 32
ota_password: "REPLACE_WITH_OTA_PASSWORD"
fallback_password: "REPLACE_WITH_AT_LEAST_8_CHARACTERS"
